# Test Project

## Introduction

This is a sample project written in Markdown to demonstrate the portfolio system. You can write normal text, **bold text**, *italic text*, and `code snippets`.

## Features

- Easy to write
- Fast to render
- No compilation needed
- Supports code blocks
- Supports math with LaTeX syntax

## Code Example

```javascript
function greet(name) {
  return `Hello, ${name}!`;
}

console.log(greet('World'));
```

## Math Example

You can write inline math like $E = mc^2$ or display math:

$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$

## Implementation Details

Here's how you might implement a simple algorithm:

1. Initialize variables
2. Process data
3. Return results

```python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)
```

## Conclusion

Markdown is simple and effective for technical documentation!
